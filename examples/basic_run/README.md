# Basic Run

Start the daemon:

```bash
uv run agentrt-core
```

In another terminal:

```bash
uv run agentrt ping
uv run agentrt run --goal "Inspect this repository and summarize the project structure"
```

Open the TUI:

```bash
uv run agentrt-tui
```
